
services:
  aira-backend:
    build: .
    ports:
      - "9000:9000"
     
  aira-wakeword:
    build: ./aira-wakeword/
    ports:
      - "5000:5000"

include:
  - path: ./aira-express/chromadb-compose.yml
